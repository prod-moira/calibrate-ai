{
  "name": "@calibrate/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types for Calibrate (backend + frontend)",
  "main": "./types.ts",
  "types": "./types.ts"
}
